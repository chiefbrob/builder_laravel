// import Multiselect from 'vue-multiselect';

// app.component('clipboard', require('./components/shared/Clipboard').default);

// app.component('multiselect', Multiselect);

// app.component('nav-root', require('./components/nav/NavRoot').default);

// app.component('file-uploader', require('./components/shared/FileUploader').default);

// app.component('field-error', require('./components/shared/FieldError').default);

// app.component('avatar', require('./components/home/ProfileImage').default);

// app.component('page-footer', require('./components/shared/Footer').default);

// app.component('phone-number', require('./components/shared/PhoneNumber').default);
